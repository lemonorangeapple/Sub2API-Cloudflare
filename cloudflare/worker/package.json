{
    "name": "sub2api-cloudflare-worker",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=22.6.0"
    },
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "test": "node --experimental-strip-types --test",
        "typecheck": "tsc --noEmit",
        "check:wrangler": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
        "check": "npm run typecheck && npm test && npm run check:wrangler"
    },
    "dependencies": {
        "bcryptjs": "3.0.3"
    },
    "devDependencies": {
        "typescript": "~5.6.3",
        "wrangler": "^4.65.0"
    }
}
